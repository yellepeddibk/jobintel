alembic>=1.12
fastapi
uvicorn[standard]
sqlalchemy>=2.0
pydantic-settings
python-dotenv
requests>=2.31
streamlit>=1.30
psycopg[binary]>=3.1
pandas
plotly
# Keep JobIntel editable so runtime imports resolve to the current checkout
# instead of a copied package snapshot that can drift from it.
-e .
