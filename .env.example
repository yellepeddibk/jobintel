# Local dev
DATABASE_URL=postgresql+psycopg://jobintel:jobintel_dev_password@127.0.0.1:5433/jobintel

# Environment: development | test | production
# - development: default for local dev work (sample ETL allowed)
# - test: for running tests (sample ETL allowed)  
# - production: for real data (sample ETL blocked, dashboard shows only production data)
# NOTE: Set ENV=production on Streamlit Cloud via secrets, not here
ENV=development

POSTGRES_DB=jobintel
POSTGRES_USER=jobintel
POSTGRES_PASSWORD=jobintel_dev_password
POSTGRES_PORT=5433
