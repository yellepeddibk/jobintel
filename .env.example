# Local default (no Docker required)
DATABASE_URL=sqlite:///./jobintel.db

# Postgres example (enable later when Docker is installed)
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/jobintel
